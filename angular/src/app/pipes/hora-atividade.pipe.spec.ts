import { HoraAtividadePipe } from './hora-atividade.pipe';

describe('HoraAtividadePipe', () => {
  it('create an instance', () => {
    const pipe = new HoraAtividadePipe();
    expect(pipe).toBeTruthy();
  });
});
