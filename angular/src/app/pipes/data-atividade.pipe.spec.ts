import { DataAtividadePipe } from './data-atividade.pipe';

describe('DataAtividadePipe', () => {
  it('create an instance', () => {
    const pipe = new DataAtividadePipe();
    expect(pipe).toBeTruthy();
  });
});
